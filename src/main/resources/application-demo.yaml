# Demo 环境配置：连接 docker-compose 起的全套中间件（MySQL 3307 / Redis 6370 / Memcached 11211 / Canal 11111）
# 启动：docker compose up -d && mvn spring-boot:run -Dspring-boot.run.profiles=demo
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/token_platform?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
